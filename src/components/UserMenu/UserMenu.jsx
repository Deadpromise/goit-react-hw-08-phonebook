import { UserMenuContainer } from './UserMenu.styled';

export const UserMenu = () => {
  return (
    <UserMenuContainer>
      <p>mango@mail.com</p>
      <button>Logout</button>
    </UserMenuContainer>
  );
};
